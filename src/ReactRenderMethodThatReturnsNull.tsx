import * as React from "react";

/**
 * Unfortunately, this file only shows phantom errors when in the context of my larger project
 */
export class ReactRenderMethodThatReturnsNull extends React.Component<any, any> {
    render() {
        return null;
    }
}
